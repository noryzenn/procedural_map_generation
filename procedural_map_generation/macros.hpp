#ifndef MACROS_HPP
#define MACROS_HPP

constexpr int WIDTH = 800;
constexpr int HEIGHT = 600;
constexpr int CELL_SIZE = 20;
constexpr int MAX_SIZE = (WIDTH / CELL_SIZE) * (HEIGHT / CELL_SIZE); 

#endif